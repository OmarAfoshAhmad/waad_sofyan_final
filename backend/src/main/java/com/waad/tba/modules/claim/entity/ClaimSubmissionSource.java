package com.waad.tba.modules.claim.entity;

/** Trusted origin assigned by the backend, never accepted from the client. */
public enum ClaimSubmissionSource {
    INTERNAL_DIRECT,
    PROVIDER_PORTAL
}
