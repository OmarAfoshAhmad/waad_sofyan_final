
# Prompt احترافي لبناء نظام مستقل لتصنيف قوائم الأسعار الطبية

أنت مهندس برمجيات خبير في Django وPython ومعالجة البيانات الطبية وتصنيف قوائم الأسعار. أريد منك بناء نظام مستقل بالكامل لمعالجة وتصنيف قوائم أسعار الخدمات الطبية القادمة من المستشفيات والمختبرات ومراكز الأشعة والعيادات والمرافق الصحية.

يجب تنفيذ المشروع كأداة مستقلة تماماً عن منظومة Waad الحالية، ويجب ألا يتضمن أي منطق متعلق بالتغطية التأمينية أو المطالبات أو حساب نسب التحمل أو الحدود المالية.

## 1. الهدف الرئيسي

بناء نظام مستقل باسم:

```text
medical_catalog_processor
```

تكون وظيفته:

1. استيراد ملفات Excel غير المنظمة القادمة من المرافق الصحية.
2. اكتشاف الأعمدة المهمة تلقائياً.
3. تنظيف وتطبيع أسماء الخدمات الطبية.
4. اكتشاف الخدمات المتكررة والمتعارضة.
5. تصنيف الخدمات طبياً وفق قائمة تصنيفات معتمدة يتم استيرادها من ملف Excel مرجعي.
6. حساب درجة ثقة لكل نتيجة تصنيف.
7. إحالة الحالات غير الواضحة للمراجعة البشرية.
8. حفظ قرارات التصنيف السابقة لإعادة استخدامها.
9. تصدير ملف Excel وJSON موحد وجاهز للاستيراد في منظومة Waad.
10. توفير سجل كامل لكل عملية استيراد وتصنيف وتعديل واعتماد.

## 2. حدود النظام

النظام متخصص فقط في:

- معالجة ملفات قوائم الأسعار.
- تنظيف البيانات.
- تصنيف الخدمات الطبية.
- توحيد أسماء الخدمات.
- اكتشاف التكرار والتعارض.
- مراجعة واعتماد نتائج التصنيف.
- تصدير البيانات النهائية.

ممنوع إضافة أي وظائف تتعلق بما يلي:

- محاكاة التغطية التأمينية.
- نسب تحمل المريض.
- نسبة تغطية شركة التأمين.
- الحدود السنوية أو المالية.
- المحافظ المالية.
- المطالبات الطبية.
- الموافقات المسبقة.
- الرفض الجزئي أو الكلي.
- تسويات مقدمي الخدمة.
- حساب المبالغ المعتمدة.
- حساب خصومات مقدم الخدمة.
- قواعد وثائق التأمين.

لا تستخدم أسماء مثل:

- `CoverageSimulation`
- `CoverageEngine`
- `InsuranceSimulation`
- `ClaimSimulation`

لأن هذه الوظائف ليست ضمن نطاق المشروع.

## 3. بنية المشروع

أنشئ المشروع في مستودع Git مستقل عن منظومة Waad.

البنية المقترحة:

```text
medical-catalog-processor/
├── config/
├── apps/
│   ├── accounts/
│   ├── facilities/
│   ├── taxonomy/
│   ├── imports/
│   ├── normalization/
│   ├── classification/
│   ├── reviews/
│   ├── exports/
│   └── audit/
├── core/
├── templates/
├── static/
├── media/
├── tests/
├── docker/
├── requirements/
├── manage.py
├── Dockerfile
├── docker-compose.yml
├── .env.example
└── README.md
```

استخدم:

- Python 3.12 أو أحدث إصدار مستقر متوافق.
- Django بأحدث إصدار مستقر.
- PostgreSQL كقاعدة بيانات مستقلة.
- Pandas لمعالجة ملفات Excel.
- OpenPyXL للقراءة والكتابة المتقدمة.
- HTMX وDjango Templates للواجهة.
- Bootstrap أو واجهة بسيطة احترافية تدعم RTL.
- Celery مع Redis للمهام الطويلة إن كانت ملفات الاستيراد كبيرة.
- Pytest لاختبارات النظام.
- Docker وDocker Compose لتشغيل المشروع.

لا تستخدم SQLite إلا داخل الاختبارات المحلية البسيطة، ولا تستخدمها في بيئة الإنتاج.

## 4. قاعدة البيانات

أنشئ النماذج التالية مع العلاقات والفهارس والقيود المناسبة.

### Facility

يمثل المرفق الصحي.

الحقول:

- `id`
- `code`
- `name_ar`
- `name_en`
- `facility_type`
- `is_active`
- `created_at`
- `updated_at`

أنواع المرافق المقترحة:

- Hospital
- Clinic
- Laboratory
- RadiologyCenter
- DentalCenter
- PhysiotherapyCenter
- Pharmacy
- MedicalSupplier
- Other

### ImportJob

يمثل عملية استيراد ملف.

الحقول:

- `id`
- `facility`
- `original_filename`
- `stored_file`
- `file_hash`
- `status`
- `total_rows`
- `valid_rows`
- `invalid_rows`
- `processed_rows`
- `classified_rows`
- `review_required_rows`
- `unclassified_rows`
- `duplicate_rows`
- `failed_rows`
- `column_mapping`
- `import_settings`
- `started_at`
- `completed_at`
- `created_at`
- `created_by`

الحالات:

- Uploaded
- MappingRequired
- Ready
- Processing
- Completed
- CompletedWithWarnings
- Failed
- Cancelled

### ImportedService

يحفظ السطر الأصلي كما ورد في الملف.

الحقول:

- `id`
- `import_job`
- `row_number`
- `raw_service_code`
- `raw_service_name`
- `raw_price`
- `raw_category`
- `raw_department`
- `raw_specialty`
- `raw_unit`
- `raw_data_json`
- `validation_status`
- `validation_errors`
- `created_at`

يجب الاحتفاظ دائماً بالقيم الأصلية وعدم تعديلها.

### NormalizedService

يحفظ نتيجة التطبيع.

الحقول:

- `id`
- `imported_service`
- `normalized_name`
- `normalized_name_ar`
- `normalized_name_en`
- `normalized_code`
- `normalized_price`
- `detected_language`
- `extracted_tokens`
- `detected_abbreviations`
- `normalization_version`
- `normalization_notes`
- `created_at`

### MedicalCategory

يمثل التصنيفات الطبية المعتمدة.

الحقول:

- `id`
- `code`
- `name_ar`
- `name_en`
- `description_ar`
- `description_en`
- `parent`
- `level`
- `display_order`
- `is_active`
- `created_at`
- `updated_at`

يجب دعم التصنيفات الرئيسية والفرعية دون فرض عدد ثابت من المستويات.

### MedicalSynonym

الحقول:

- `id`
- `term`
- `normalized_term`
- `language`
- `category`
- `service_mapping`
- `synonym_type`
- `priority`
- `is_active`

أنواع المرادفات:

- ExactName
- Abbreviation
- Translation
- AlternativeSpelling
- CommonName
- MedicalName

### ClassificationRule

الحقول:

- `id`
- `name`
- `description`
- `pattern`
- `match_type`
- `category`
- `facility`
- `facility_type`
- `specialty`
- `required_terms`
- `excluded_terms`
- `priority`
- `confidence_score`
- `is_global`
- `is_active`
- `created_at`
- `updated_at`

أنواع المطابقة:

- Exact
- Contains
- StartsWith
- EndsWith
- Regex
- TokenSet
- Fuzzy

### ServiceMapping

يمثل خدمة مصنفة ومعتمدة سابقاً.

الحقول:

- `id`
- `facility`
- `provider_service_code`
- `original_service_name`
- `normalized_service_name`
- `medical_category`
- `source`
- `confidence_score`
- `is_verified`
- `usage_count`
- `last_used_at`
- `approved_by`
- `approved_at`
- `created_at`
- `updated_at`

مصادر التصنيف:

- Manual
- ExactMapping
- Rule
- Semantic
- AIModel
- ImportedReference

### ClassificationResult

الحقول:

- `id`
- `normalized_service`
- `predicted_category`
- `confidence_score`
- `classification_method`
- `classification_details`
- `candidate_categories`
- `rule`
- `service_mapping`
- `model_name`
- `model_version`
- `needs_review`
- `status`
- `created_at`

الحالات:

- AutoApproved
- Suggested
- NeedsReview
- Approved
- Corrected
- Rejected
- Unclassified
- Conflicted

### ClassificationReview

الحقول:

- `id`
- `classification_result`
- `predicted_category`
- `approved_category`
- `decision`
- `review_note`
- `reviewed_by`
- `reviewed_at`

القرارات:

- Approve
- Correct
- Reject
- Defer

### ExportBatch

الحقول:

- `id`
- `import_job`
- `export_format`
- `schema_version`
- `file`
- `total_services`
- `approved_services`
- `excluded_services`
- `export_settings`
- `created_by`
- `created_at`

### AuditLog

الحقول:

- `id`
- `user`
- `action`
- `entity_type`
- `entity_id`
- `before_data`
- `after_data`
- `reason`
- `ip_address`
- `created_at`

يجب تسجيل:

- رفع الملفات.
- تعديل ربط الأعمدة.
- تشغيل التصنيف.
- تعديل التصنيف.
- الاعتماد الجماعي.
- إنشاء أو تعديل قاعدة.
- إضافة مرادف.
- تصدير الملف.
- حذف أو تعطيل أي سجل.

## 5. مسار الاستيراد

نفذ معالج استيراد متعدد المراحل.

### المرحلة 1: رفع الملف

يدعم النظام:

- `.xlsx`
- `.xls`
- `.csv`

يجب التحقق من:

- نوع الملف.
- الحجم الأقصى.
- سلامة الملف.
- وجود بيانات.
- عدم تكرار الملف باستخدام `file_hash`.
- منع رفع ملفات تنفيذية أو ضارة.

### المرحلة 2: معاينة الملف

بعد رفع الملف:

- اعرض أسماء الأوراق.
- اسمح باختيار الورقة.
- اعرض أول 20 إلى 50 صفاً.
- اكتشف صف العناوين تلقائياً.
- تجاهل الصفوف الفارغة والعناوين التوضيحية.
- لا تفترض أن أول صف هو صف العناوين.

### المرحلة 3: اكتشاف الأعمدة

حاول اكتشاف الأعمدة التالية:

- كود الخدمة.
- اسم الخدمة.
- السعر.
- التصنيف القديم.
- القسم.
- التخصص.
- الوحدة.
- الملاحظات.

دعم الأسماء العربية والإنجليزية مثل:

```text
اسم الخدمة
الخدمة
البيان
الوصف
Service Name
Description
Item
Procedure

السعر
القيمة
Price
Rate
Amount
Contract Price
```

اعرض نتيجة الاكتشاف للمستخدم واسمح له بتعديل ربط الأعمدة قبل المتابعة.

احفظ ربط الأعمدة لكل مرفق حتى يمكن إعادة استخدامه مستقبلاً.

### المرحلة 4: التحقق من البيانات

تحقق من:

- وجود اسم الخدمة.
- صلاحية السعر.
- عدم وجود أسعار سالبة.
- عدم وجود أكواد مكررة متعارضة.
- الصفوف المكررة.
- الحقول المفقودة.
- الخلايا المدمجة.
- القيم النصية داخل عمود السعر.
- تعدد العملات أو وجود رموز عملات.
- وجود أكثر من سعر في السطر.

لا تحذف أي صف بصمت.

كل صف غير صالح يجب الاحتفاظ به مع سبب الخطأ.

## 6. تطبيع أسماء الخدمات الطبية

أنشئ محرك تطبيع مستقل قابل للاختبار والإصدار.

يجب أن يقوم بما يلي:

- تحويل النص إلى Unicode موحد.
- إزالة المسافات المتكررة.
- إزالة الرموز غير المهمة.
- توحيد الأرقام العربية والإنجليزية.
- توحيد الهمزات.
- معالجة التاء المربوطة والياء والألف المقصورة بحذر.
- إزالة التطويل.
- معالجة الأحرف الكبيرة والصغيرة في الإنجليزية.
- توحيد علامات الشرطة والشرط المائل.
- فصل الكلمات الملتصقة.
- إزالة كلمات السعر والعملة والكمية من اسم الخدمة.
- اكتشاف الاختصارات الطبية.
- توحيد الاختصارات الطبية المعروفة.
- اكتشاف اللغة: عربي، إنجليزي، مختلط.
- الحفاظ على الاتجاهات الطبية المهمة مثل:
  - Left
  - Right
  - Bilateral
  - With Contrast
  - Without Contrast
  - Upper
  - Lower
  - Complete
  - Partial
  - Adult
  - Pediatric

لا تحذف الكلمات التي قد تغير المعنى الطبي.

أمثلة يجب التعامل معها:

```text
CBC
C.B.C
Complete Blood Count
Complete Blood Picture
صورة دم كاملة
عد دم كامل
```

يجب التعرف عليها كخدمات متقاربة دلالياً.

لكن يجب عدم دمج خدمات مختلفة طبياً لمجرد التشابه النصي.

مثلاً:

```text
CT Brain
CT Guided Biopsy
```

لا تعتبران الخدمة نفسها.

## 7. محرك التصنيف الطبي

نفذ محركاً هجينا متعدد المراحل.

ترتيب مراحل التصنيف:

### المستوى 1: التطابق بالكود

إذا كان كود الخدمة مرتبطاً سابقاً بنفس المرفق بتصنيف معتمد، استخدمه بأعلى أولوية.

### المستوى 2: التطابق المباشر

ابحث عن:

- الاسم الأصلي.
- الاسم المطبع.
- المرادفات.
- الترجمات.
- الخدمات المعتمدة سابقاً.

### المستوى 3: القواعد الطبية

طبق القواعد حسب الأولوية.

يجب دعم:

- الكلمات المطلوبة.
- الكلمات المستثناة.
- نوع المرفق.
- التخصص.
- القسم.
- اللغة.
- أكثر من شرط.
- الاستثناءات.
- الأولوية.

مثال:

```text
إذا احتوى الاسم على MRI أو Magnetic Resonance
→ الرنين المغناطيسي
```

لكن يجب الانتباه إلى الإجراءات المركبة.

مثال:

```text
MRI Guided Biopsy
```

قد يحتاج إلى تصنيف مختلف عن فحص MRI عادي.

### المستوى 4: المطابقة المتقاربة

استخدم مطابقة نصية مثل:

- RapidFuzz
- Token Sort Ratio
- Token Set Ratio
- Levenshtein

لا تعتمد المطابقة المتقاربة وحدها للتصنيف التلقائي إلا عند وجود نتيجة قوية جداً ومعتمدة سابقاً.

### المستوى 5: المطابقة الدلالية

اجعل طبقة الـ Semantic Classification قابلة للاستبدال.

يمكن استخدام:

- Sentence Transformers.
- نموذج متعدد اللغات.
- Embeddings محلية.
- `pgvector` داخل PostgreSQL عند الحاجة.

قارن الخدمة مع:

- أسماء التصنيفات.
- وصف التصنيفات.
- المرادفات.
- الخدمات المعتمدة سابقاً.
- أمثلة كل تصنيف.

أعد أفضل خمسة تصنيفات مرشحة مع درجة لكل تصنيف.

### المستوى 6: نموذج طبي أو نموذج لغوي

استخدم النموذج فقط في الحالات التي لم تحسمها المراحل السابقة.

يجب ألا يسمح للنموذج باختراع تصنيف جديد.

يجب إرسال:

- اسم الخدمة الأصلي.
- الاسم المطبع.
- كود الخدمة.
- القسم.
- التخصص.
- نوع المرفق.
- أفضل التصنيفات المرشحة.
- أسماء التصنيفات وأكوادها وتعريفاتها.
- أمثلة معتمدة مرتبطة بها.

اطلب من النموذج إعادة JSON منظم فقط:

```json
{
  "selectedCategoryCode": "CATEGORY_CODE",
  "confidence": 0.91,
  "reason": "شرح مختصر",
  "alternatives": [
    {
      "categoryCode": "OTHER_CODE",
      "confidence": 0.72
    }
  ]
}
```

تحقق من أن الكود المختار موجود فعلاً في قاعدة البيانات.

أي استجابة غير صالحة يجب ألا تعتمد.

## 8. حساب الثقة

لا تعتمد مباشرة درجة الثقة الصادرة من نموذج الذكاء الاصطناعي.

احسب درجة نهائية مركبة تعتمد على:

- تطابق كود الخدمة.
- تطابق الاسم الكامل.
- تطابق الاسم المطبع.
- وجود خدمة معتمدة سابقاً.
- نتيجة القاعدة الطبية.
- التشابه النصي.
- التشابه الدلالي.
- توافق التخصص.
- توافق نوع المرفق.
- عدد المرشحين المتقاربين.
- وجود تعارض بين القواعد.
- مصدر النتيجة.

السياسة المقترحة:

- `0.95` فأكثر: اعتماد تلقائي عند عدم وجود تعارض.
- من `0.85` إلى أقل من `0.95`: اقتراح قوي قابل للمراجعة.
- من `0.65` إلى أقل من `0.85`: مراجعة بشرية إلزامية.
- أقل من `0.65`: غير مصنف.
- وجود تعارض: مراجعة بشرية إلزامية مهما كانت الدرجة.
- أي تصنيف طبي حساس أو مركب يمكن ضبطه ليتطلب مراجعة دائمة.

اجعل الحدود قابلة للتعديل من الإعدادات.

## 9. التكرار والتعارض

يجب اكتشاف:

- تكرار الاسم والكود والسعر.
- تكرار الكود مع أسماء مختلفة.
- تكرار الاسم مع أسعار مختلفة.
- خدمات متقاربة جداً في الاسم.
- نفس الخدمة بتصنيفين مختلفين.
- نفس الكود مستخدماً في أكثر من خدمة.
- اختلافات طفيفة في الكتابة.
- تكرار داخل الملف.
- تكرار مع ملفات سابقة لنفس المرفق.

صنف الحالات إلى:

- ExactDuplicate
- PossibleDuplicate
- CodeConflict
- PriceConflict
- CategoryConflict
- NameVariation
- PreviousMappingConflict

لا تدمج أو تحذف الخدمات تلقائياً دون قرار واضح.

## 10. المراجعة البشرية

أنشئ واجهة مراجعة مخصصة باستخدام Django Templates وHTMX.

يجب أن تحتوي على:

- اسم الخدمة الأصلي.
- الاسم بعد التطبيع.
- كود الخدمة.
- السعر.
- التصنيف المقترح.
- درجة الثقة.
- طريقة التصنيف.
- سبب التصنيف.
- القاعدة المستخدمة.
- أفضل التصنيفات البديلة.
- الخدمات المشابهة المعتمدة سابقاً.
- بيانات المرفق والقسم والتخصص.
- أزرار اعتماد أو تعديل أو رفض أو تأجيل.

يجب دعم:

- البحث.
- الفرز.
- الفلترة حسب المرفق.
- الفلترة حسب التصنيف.
- الفلترة حسب درجة الثقة.
- الفلترة حسب حالة المراجعة.
- الفلترة حسب طريقة التصنيف.
- الاعتماد الجماعي.
- تعديل جماعي للتصنيف.
- تحديد عدة خدمات.
- الانتقال إلى الخدمة التالية بعد الاعتماد.
- اختصارات لوحة المفاتيح إن أمكن.

عند تصحيح التصنيف:

- احفظ القرار.
- حدث أو أنشئ `ServiceMapping`.
- اقترح إنشاء مرادف أو قاعدة.
- لا تنشئ قاعدة عامة تلقائياً دون موافقة المستخدم.
- سجل التغيير داخل `AuditLog`.

## 11. واجهة النظام

أنشئ لوحة تحكم عربية RTL احترافية.

الصفحات الأساسية:

1. تسجيل الدخول.
2. لوحة التحكم.
3. المرافق الصحية.
4. رفع ملف جديد.
5. معاينة الملف وربط الأعمدة.
6. متابعة حالة المعالجة.
7. نتائج الاستيراد.
8. شاشة مراجعة التصنيفات.
9. شاشة التكرارات والتعارضات.
10. إدارة التصنيفات الطبية.
11. إدارة المرادفات.
12. إدارة قواعد التصنيف.
13. الخدمات المعتمدة سابقاً.
14. عمليات التصدير.
15. سجل التدقيق.
16. إعدادات النظام.

إحصائيات لوحة التحكم:

- عدد الملفات المستوردة.
- إجمالي الخدمات.
- المصنف تلقائياً.
- المعتمد يدوياً.
- قيد المراجعة.
- غير المصنف.
- المتعارض.
- المكرر.
- متوسط درجة الثقة.
- التصنيفات الأكثر استخداماً.
- المرافق الأكثر رفعاً للملفات.

Django Admin يستخدم للإدارة التقنية فقط، ولا تعتمد عليه كواجهة التشغيل الرئيسية.

## 12. التصنيفات المرجعية

يجب توفير وظيفة لاستيراد التصنيفات من ملف Excel مرجعي.

يدعم الملف أعمدة مثل:

- كود التصنيف.
- اسم التصنيف العربي.
- اسم التصنيف الإنجليزي.
- التصنيف الأب.
- المستوى.
- الوصف.
- حالة التفعيل.

يجب:

- إجراء Preview قبل الاستيراد.
- اكتشاف الأكواد المكررة.
- اكتشاف الأسماء المتعارضة.
- عدم حذف تصنيف مستخدم.
- دعم التحديث الآمن.
- تسجيل كل تغيير.
- إصدار نسخة من قاموس التصنيفات.

لا تنشئ تصنيفات جديدة من تلقاء نفسك.

التصنيفات الوحيدة المسموح بها هي التصنيفات المعتمدة في الملف المرجعي أو التي يضيفها مستخدم مخول.

## 13. التصدير

يدعم النظام:

- Excel.
- JSON.
- CSV اختياري.

صيغة التصدير الأساسية:

```text
provider_service_code
provider_service_name
normalized_service_name
price
currency
category_code
category_name_ar
category_name_en
classification_confidence
classification_method
review_status
reviewed_by
reviewed_at
notes
```

صيغة JSON:

```json
{
  "schemaVersion": "1.0",
  "facility": {
    "code": "FAC-001",
    "name": "اسم المرفق"
  },
  "generatedAt": "ISO_DATE",
  "taxonomyVersion": "1.0",
  "services": [
    {
      "providerServiceCode": "CODE",
      "providerServiceName": "Original Name",
      "normalizedServiceName": "Normalized Name",
      "price": 0.00,
      "currency": "LYD",
      "categoryCode": "CATEGORY_CODE",
      "categoryNameAr": "اسم التصنيف",
      "categoryNameEn": "Category Name",
      "classificationConfidence": 0.98,
      "classificationMethod": "ExactMapping",
      "reviewStatus": "Approved"
    }
  ]
}
```

يجب تضمين:

- `schemaVersion`
- إصدار التصنيفات.
- تاريخ الإنشاء.
- رمز المرفق.
- عدد الخدمات.
- عدد الخدمات المستبعدة.
- تقرير الأخطاء.

يجب ألا يتم تصدير خدمات بالحالات التالية داخل ملف الإنتاج النهائي:

- Unclassified
- Conflicted
- NeedsReview
- Invalid
- MissingName
- InvalidPrice

يمكن تصديرها في ملف أخطاء منفصل.

## 14. الأمان والصلاحيات

أنشئ أدواراً واضحة:

- SystemAdmin
- TaxonomyManager
- MedicalReviewer
- DataOperator
- Viewer

الصلاحيات:

### SystemAdmin

- إدارة المستخدمين.
- إدارة الإعدادات.
- إدارة كل البيانات.

### TaxonomyManager

- إدارة التصنيفات.
- إدارة المرادفات.
- إدارة القواعد.
- اعتماد التعديلات الهيكلية.

### MedicalReviewer

- مراجعة واعتماد وتصحيح التصنيفات.
- اقتراح قواعد ومرادفات.

### DataOperator

- رفع الملفات.
- ربط الأعمدة.
- تشغيل المعالجة.
- تصدير النتائج المعتمدة.

### Viewer

- الاطلاع على النتائج والتقارير فقط.

يجب منع أي مستخدم من تنفيذ وظيفة خارج صلاحياته.

## 15. الأداء والمهام الطويلة

لا تنفذ معالجة ملف كبير داخل طلب HTTP واحد.

استخدم مهام خلفية لمعالجة:

- قراءة الملف.
- التطبيع.
- التصنيف.
- اكتشاف التكرار.
- التصدير.

يجب:

- إظهار نسبة التقدم.
- حفظ حالة المهمة.
- دعم إعادة المحاولة.
- عدم تكرار المعالجة عند إعادة تشغيل المهمة.
- تسجيل الخطأ مع رقم الصف.
- عدم فقد النتائج السابقة عند حدوث خطأ جزئي.
- معالجة الملفات على دفعات.

اجعل المعالجة Idempotent قدر الإمكان.

## 16. الاختبارات

أنشئ اختبارات شاملة.

### اختبارات الوحدة

- تطبيع العربية.
- تطبيع الإنجليزية.
- الاختصارات.
- قواعد التصنيف.
- حساب الثقة.
- اكتشاف التكرار.
- التحقق من السعر.
- ربط الأعمدة.
- التصدير.

### اختبارات التكامل

- رفع ملف كامل.
- اكتشاف الأعمدة.
- تشغيل المعالجة.
- تصنيف الخدمات.
- المراجعة.
- التصدير.
- الصلاحيات.
- سجل التدقيق.

### حالات اختبار مهمة

- ملف عربي.
- ملف إنجليزي.
- ملف عربي وإنجليزي مختلط.
- عناوين تبدأ بعد عدة صفوف.
- أكثر من ورقة.
- صفوف مدمجة.
- أسعار نصية.
- أكواد مكررة.
- خدمات بدون أكواد.
- خدمات بدون أسعار.
- خدمات مكررة بأسعار مختلفة.
- اختصارات طبية.
- أسماء بها أخطاء إملائية.
- خدمات متشابهة نصياً ومختلفة طبياً.
- ملف يحتوي آلاف الخدمات.

## 17. معايير القبول

يعتبر النظام ناجحاً عند تحقيق ما يلي:

- عدم فقد أي صف أثناء الاستيراد.
- الحفاظ على القيمة الأصلية لكل خلية.
- نقل الأسعار والأكواد بدقة 100%.
- اكتشاف جميع الصفوف غير الصالحة وإظهار أسبابها.
- منع التصدير النهائي للحالات غير المعتمدة.
- إمكانية تفسير سبب كل تصنيف.
- إمكانية معرفة القاعدة أو المطابقة أو النموذج الذي أنتج النتيجة.
- إمكانية إعادة إنتاج نفس النتيجة باستخدام نفس إصدار القواعد والتصنيفات.
- وجود سجل تدقيق كامل.
- دعم مراجعة بشرية فعالة.
- عدم وجود أي منطق تأميني داخل النظام.
- عدم وجود اتصال مباشر بقاعدة بيانات Waad.
- نجاح الاختبارات الآلية الأساسية.
- تشغيل المشروع بالكامل من خلال Docker Compose.

الهدف المبدئي لدقة التصنيف:

- دقة إجمالية لا تقل عن 95% على ملف مرجعي معتمد.
- دقة لا تقل عن 98% للتصنيفات التي تعتمد تلقائياً.
- جميع الحالات منخفضة الثقة تدخل المراجعة البشرية.
- لا يتم اعتبار أي درجة دقة صحيحة قبل مقارنتها ببيانات مصنفة يدوياً ومعتمدة.

## 18. التكامل مع منظومة Waad

ممنوع الاتصال المباشر بقاعدة بيانات Waad.

التكامل يكون فقط من خلال:

- ملف Excel موحد.
- ملف JSON بإصدار واضح.
- API مستقل مستقبلاً عند الحاجة.

يجب إنشاء وثيقة:

```text
DATA_CONTRACT.md
```

توضح:

- أسماء الحقول.
- أنواعها.
- الحقول الإجبارية.
- القيم المسموح بها.
- صيغة الأكواد.
- صيغة الأسعار.
- حالات المراجعة.
- سياسة الإصدارات.
- كيفية التعامل مع التغييرات المستقبلية.

## 19. عدم حذف كود Waad حالياً

لا تحذف أي كود من منظومة Waad في هذه المرحلة.

نفذ أولاً النظام المستقل واختبره.

بعد اكتماله يجب إعداد وثيقة منفصلة باسم:

```text
WAAD_DECOMMISSION_PLAN.md
```

تتضمن:

1. جرد الأكواد القديمة.
2. تحديد Controllers وServices وEntities وRepositories.
3. تحديد شاشات وروابط الواجهة.
4. تحديد الجداول وملفات Flyway.
5. تشغيل النظامين بالتوازي.
6. مقارنة النتائج.
7. تعطيل الوظائف القديمة باستخدام Feature Flag.
8. أخذ نسخة احتياطية.
9. حذف الكود في Pull Request مستقل.
10. حذف الجداول في Migration مستقلة لاحقة.
11. توفير خطة Rollback.

لا تستخدم أسلوب الحذف المباشر.

## 20. مراحل التنفيذ المطلوبة

نفذ المشروع على مراحل، ولا تكتب النظام بالكامل في ملف واحد.

### المرحلة 1: التأسيس

- إنشاء المشروع.
- إعداد PostgreSQL.
- إعداد Docker.
- إعداد المتغيرات البيئية.
- إنشاء المستخدمين والصلاحيات.
- إنشاء التطبيقات الأساسية.
- إنشاء README.

### المرحلة 2: قاعدة البيانات

- إنشاء النماذج.
- إنشاء Migrations.
- إنشاء الفهارس والقيود.
- إنشاء Django Admin للإدارة التقنية.

### المرحلة 3: استيراد الملفات

- رفع الملف.
- المعاينة.
- اكتشاف الأعمدة.
- ربط الأعمدة.
- التحقق من البيانات.
- حفظ الصفوف الأصلية.

### المرحلة 4: التطبيع

- إنشاء محرك التطبيع.
- كتابة الاختبارات.
- إصدار نسخة لمحرك التطبيع.

### المرحلة 5: التصنيف

- المطابقة بالكود.
- المطابقة المباشرة.
- المرادفات.
- القواعد.
- التشابه النصي.
- المطابقة الدلالية.
- طبقة النموذج الاختيارية.
- حساب الثقة.

### المرحلة 6: المراجعة

- واجهة المراجعة.
- الاعتماد والتصحيح.
- الاعتماد الجماعي.
- التعلم من القرارات السابقة.

### المرحلة 7: التكرار والتعارض

- اكتشاف التكرارات.
- اكتشاف تعارض الأكواد.
- اكتشاف تعارض الأسعار والتصنيفات.
- واجهة المعالجة.

### المرحلة 8: التصدير

- Excel.
- JSON.
- تقرير الأخطاء.
- عقد البيانات.

### المرحلة 9: الاختبارات والتوثيق

- اختبارات الوحدة.
- اختبارات التكامل.
- بيانات تجريبية.
- دليل التشغيل.
- دليل المستخدم.
- دليل النشر.

## 21. أسلوب التنفيذ البرمجي

التزم بما يلي:

- Clean Architecture قدر الإمكان.
- فصل منطق الأعمال عن Views وModels.
- استخدام Services واضحة.
- استخدام Type Hints.
- عدم تكرار الكود.
- عدم وضع منطق التصنيف داخل Django Views.
- عدم وضع كل شيء داخل `models.py`.
- إنشاء وحدات مستقلة للتطبيع والتصنيف والتصدير.
- استخدام معاملات قاعدة البيانات عند الحاجة.
- استخدام `Decimal` للأسعار، وعدم استخدام `float`.
- التحقق من جميع المدخلات.
- رسائل خطأ واضحة بالعربية.
- دعم RTL.
- عدم إخفاء الأخطاء.
- عدم حذف البيانات الأصلية.
- كتابة Docstrings للأجزاء المهمة.
- إضافة Logging منظم.
- عدم تخزين كلمات المرور أو المفاتيح داخل الكود.
- إنشاء `.env.example`.
- إضافة قيود رفع الملفات.
- منع Formula Injection في ملفات Excel المصدرة.
- التعامل الآمن مع أسماء الملفات والمسارات.

## 22. المخرجات المطلوبة منك

ابدأ أولاً بإنشاء وثائق التصميم التالية:

1. `ARCHITECTURE.md`
2. `DATABASE_DESIGN.md`
3. `IMPORT_PIPELINE.md`
4. `CLASSIFICATION_ENGINE.md`
5. `DATA_CONTRACT.md`
6. `SECURITY.md`
7. `TEST_PLAN.md`
8. `DEPLOYMENT.md`
9. `WAAD_DECOMMISSION_PLAN.md`

بعد ذلك أنشئ الكود الفعلي للمرحلة الأولى والثانية.

في كل مرحلة:

- اذكر الملفات التي ستنشئها أو تعدلها.
- اشرح سبب كل قرار معماري.
- نفذ الكود كاملاً وليس أمثلة ناقصة.
- أضف الاختبارات المناسبة.
- أضف أوامر التشغيل.
- لا تستخدم بيانات وهمية داخل منطق الإنتاج.
- لا تنتقل إلى حذف أي شيء من Waad.
- لا تضف محاكاة تغطية أو وظائف تأمينية.

ابدأ الآن بتحليل المتطلبات، ثم أنشئ الهيكل المعماري وقاعدة البيانات وخطة التنفيذ التفصيلية، وبعدها ابدأ في تأسيس المشروع القابل للتشغيل باستخدام Docker وPostgreSQL.