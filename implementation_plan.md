# محاكاة التغطية واختبار جدول الوثائق (Coverage Simulation Tool)

هذه الميزة تهدف إلى توفير أداة لاختبار قوائم أسعار المرافق الطبية مقابل جداول وثائق التأمين (Benefit Policies) لمعرفة مدى دقة حساب التغطية لكل خدمة، وتمييز الخدمات الخارجة عن قواعد الوثيقة.

## User Review Required
> [!IMPORTANT]
> يرجى مراجعة الخطة أدناه وتأكيدها لنتمكن من بدء التنفيذ.

## Open Questions
> [!NOTE]
> هل ترغب في أن يتم حساب نسب التحمل (Copay) الثابتة (مثل 50 دينار كحد أقصى) ضمن هذه المحاكاة، أم نكتفي بالنسبة المئوية (Coverage Percentage) المحددة في جدول الوثيقة؟ حالياً سنطبق النسبة المئوية حسب قاعدة (BenefitPolicyRule).

## Proposed Changes

---

### Backend (الخوادم)
سيتم إنشاء خدمة جديدة ومسار (Endpoint) لإجراء عملية المحاكاة:

#### [NEW] `CoverageSimulationItemDto.java`
- سينقل بيانات كل خدمة: اسم الخدمة، كود الخدمة، السعر المعتمد، التصنيف، حالة التغطية (مغطاة، غير مغطاة)، النسبة المغطاة، قيمة التحمل (Copay)، وقيمة التغطية.

#### [NEW] `CoverageSimulationResultDto.java`
- سيشمل قائمة من الخدمات أعلاه، بالإضافة إلى ملخص (عدد الخدمات المغطاة، وغير المغطاة).

#### [NEW] `CoverageSimulationService.java`
- ستقوم هذه الخدمة بالاستعلام عن قائمة الأسعار (عبر `contractId`) والاستعلام عن جدول الوثائق (عبر `policyId`).
- مضاهاة كل خدمة بناءً على تصنيفها الطبي `MedicalCategory` بالقواعد `BenefitPolicyRule` لحساب قيم التغطية وتحديد الخدمات الخارجة عن الوثيقة.

#### [NEW] `CoverageSimulationController.java`
- توفير المسار: `GET /api/simulation/coverage?contractId={id}&policyId={id}`

---

### Frontend (واجهة المستخدم)

#### [NEW] `simulation.service.js`
- للاتصال بالـ API وجلب نتائج المحاكاة.

#### [NEW] `CoverageSimulationPage.jsx`
- شاشة جديدة تحتوي على:
  - قائمتين منسدلتين لاختيار (عقد المرفق - Provider Contract) و (جدول الوثائق - Benefit Policy).
  - زر (بدء المحاكاة - Run Simulation).
  - جدول يعرض النتائج بتفاصيل (السعر، التغطية، التحمل).
  - **تمييز بصري (Highlight)**: سيتم تلوين الأسطر بخلفية حمراء/برتقالية للخدمات غير المغطاة (EXCLUDED).

#### [MODIFY] `MainRoutes.jsx`
- إضافة المسار الخاص بالشاشة الجديدة `/system-settings/coverage-simulation`.

#### [MODIFY] `systemSettings.jsx` (أو قائمة الإعدادات الحالية)
- إضافة خيار في قائمة الجانب (إعدادات النظام) باسم "اختبار محاكاة التغطية".

## Verification Plan

### Manual Verification
- سيتم التأكد من الدخول إلى (اختبار التغطية).
- اختيار عقد مرفق وتحديد وثيقة تأمين معينة.
- التأكد من أن جميع الخدمات تظهر حسابات صحيحة ومميزة لونياً إذا كانت خارج نطاق الوثيقة.
